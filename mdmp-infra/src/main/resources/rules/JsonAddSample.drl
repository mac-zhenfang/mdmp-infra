package com.mdmp.infra.rule
 
import com.mdmp.infra.messager.JsonMessage;

rule "Add"
    when
        m : JsonMessage()
    then
        System.out.println( "Rule: Add" );
        m.putValue("age", "20");
        update( m );
        m.setMessage( "Goodbye Add" );
end

